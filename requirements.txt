# Generated from requirements.in. Platform setup installs the correct PyTorch
# build first, then installs this lock with --no-deps so package metadata cannot
# replace the selected CUDA/ROCm build.
-r requirements.lock.txt
